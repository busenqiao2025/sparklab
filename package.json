{
  "name": "sparkminds-lab",
  "version": "1.5.0",
  "private": true,
  "description": "SparkMinds Lab — Cloudflare Workers 版",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
