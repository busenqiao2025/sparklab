<style>
  /* ============================================================
     SparkMinds Lab v2.0 — Design System
     Modern, clean, inspired by Linear / Vercel / Stripe
     ============================================================ */
  :root{
    /* Background layers */
    --bg:#f8f9fb;
    --bg2:#ffffff;
    --bg-soft:#f1f3f7;
    --bg-hover:#eceef3;
    /* Foreground */
    --fg:#0f172a;
    --fg-dim:#475569;
    --fg-light:#94a3b8;
    /* Accent — Indigo */
    --accent:#6366f1;
    --accent-light:#818cf8;
    --accent-soft:#eef2ff;
    --accent-dark:#4f46e5;
    /* Semantic */
    --danger:#ef4444;
    --danger-soft:#fef2f2;
    --success:#10b981;
    --success-soft:#ecfdf5;
    --warning:#f59e0b;
    --warning-soft:#fffbeb;
    --info:#0ea5e9;
    --info-soft:#f0f9ff;
    --muted:#94a3b8;
    /* Borders */
    --border:#e2e8f0;
    --border-light:#f1f5f9;
    --border-strong:#cbd5e1;
    /* Shadows — soft and layered */
    --shadow-xs:0 1px 2px rgba(15,23,42,.04);
    --shadow:0 1px 3px rgba(15,23,42,.06),0 1px 2px rgba(15,23,42,.04);
    --shadow-md:0 4px 12px -2px rgba(15,23,42,.08),0 2px 6px -2px rgba(15,23,42,.05);
    --shadow-lg:0 12px 24px -4px rgba(15,23,42,.10),0 4px 8px -2px rgba(15,23,42,.06);
    --shadow-xl:0 24px 48px -8px rgba(15,23,42,.14),0 8px 16px -4px rgba(15,23,42,.08);
    /* Radii */
    --radius:16px;
    --radius-md:12px;
    --radius-sm:10px;
    --radius-xs:7px;
    /* Typography */
    --font:'Inter',-apple-system,BlinkMacSystemFont,'Segoe UI','PingFang SC','Microsoft YaHei',sans-serif;
    --mono:'SF Mono','Fira Code','JetBrains Mono',monospace;
    /* Motion */
    --transition:all .18s cubic-bezier(.4,0,.2,1);
    --transition-slow:all .3s cubic-bezier(.4,0,.2,1);
    /* Layout */
    --topbar-h:48px;
    --sidebar-w:220px;
  }

  /* ===== Dark Mode ===== */
  [data-theme="dark"]{
    --bg:#0b0e14;
    --bg2:#141821;
    --bg-soft:#1a1f2b;
    --bg-hover:#1e2433;
    --fg:#e2e8f0;
    --fg-dim:#94a3b8;
    --fg-light:#64748b;
    --accent:#818cf8;
    --accent-light:#a5b4fc;
    --accent-soft:rgba(99,102,241,.12);
    --accent-dark:#6366f1;
    --danger:#f87171;
    --danger-soft:rgba(239,68,68,.1);
    --success:#34d399;
    --success-soft:rgba(16,185,129,.1);
    --warning:#fbbf24;
    --warning-soft:rgba(245,158,11,.1);
    --info:#38bdf8;
    --info-soft:rgba(14,165,233,.1);
    --muted:#475569;
    --border:#1e2433;
    --border-light:#1a1f2b;
    --border-strong:#334155;
    --shadow-xs:0 1px 2px rgba(0,0,0,.2);
    --shadow:0 1px 3px rgba(0,0,0,.3),0 1px 2px rgba(0,0,0,.2);
    --shadow-md:0 4px 12px -2px rgba(0,0,0,.35),0 2px 6px -2px rgba(0,0,0,.25);
    --shadow-lg:0 12px 24px -4px rgba(0,0,0,.4),0 4px 8px -2px rgba(0,0,0,.3);
    --shadow-xl:0 24px 48px -8px rgba(0,0,0,.5),0 8px 16px -4px rgba(0,0,0,.35);
  }

  /* ===== Reset ===== */
  *{box-sizing:border-box;margin:0;padding:0}
  html,body{height:100%;font-family:var(--font);font-size:13px;color:var(--fg);overflow:hidden;-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale}
  body{position:relative;background:var(--bg)}
  body::before{content:'';position:fixed;top:0;left:0;width:100%;height:100%;z-index:-2;background:var(--bg)}
  body::after{content:'';position:fixed;top:-50%;left:-50%;width:200%;height:200%;z-index:-1;
    background:
      radial-gradient(circle at 15% 20%,rgba(99,102,241,.06) 0%,transparent 35%),
      radial-gradient(circle at 85% 80%,rgba(168,85,247,.05) 0%,transparent 35%);
    animation:bgFloat 20s ease-in-out infinite alternate}
  [data-theme="dark"] body::after{
    background:
      radial-gradient(circle at 15% 20%,rgba(99,102,241,.08) 0%,transparent 35%),
      radial-gradient(circle at 85% 80%,rgba(168,85,247,.06) 0%,transparent 35%)}
  @keyframes bgFloat{0%{transform:translate(0,0)}100%{transform:translate(2%,-2%)}}
  ::-webkit-scrollbar{width:6px;height:6px}
  ::-webkit-scrollbar-track{background:transparent}
  ::-webkit-scrollbar-thumb{background:var(--border-strong);border-radius:3px}
  ::-webkit-scrollbar-thumb:hover{background:var(--muted)}
  .scanline{display:none}
  .glow{text-shadow:none}
  a{color:var(--accent);text-decoration:none}
  a:hover{text-decoration:underline}

  /* ===== Screens ===== */
  .screen{display:none;flex-direction:column;height:100vh;width:100vw;padding:0}
  .screen.active{display:flex}

  /* ===== Top Bar ===== */
  #mainScreen{flex-direction:column;overflow:hidden}
  .trae-topbar{display:flex;align-items:center;justify-content:space-between;height:var(--topbar-h);padding:0 20px;background:var(--bg2);border-bottom:1px solid var(--border);flex-shrink:0;z-index:50}
  .trae-topbar-left{display:flex;align-items:center;gap:2px}
  .trae-brand{font-size:14px;font-weight:700;color:var(--fg);letter-spacing:-.4px;display:flex;align-items:center;gap:10px;margin-right:20px;white-space:nowrap}
  .trae-brand::before{content:'';width:24px;height:24px;border-radius:7px;background:linear-gradient(135deg,var(--accent),var(--accent-dark));display:inline-block;box-shadow:var(--shadow-md);position:relative}
  .trae-cat{background:transparent;border:none;color:var(--fg-dim);font-family:var(--font);font-size:12px;padding:7px 14px;cursor:pointer;transition:var(--transition);border-radius:var(--radius-sm);font-weight:500;white-space:nowrap;letter-spacing:-.1px}
  .trae-cat:hover{background:var(--bg-hover);color:var(--fg)}
  .trae-cat.active{background:var(--accent-soft);color:var(--accent);font-weight:600}
  .trae-topbar-right{display:flex;align-items:center;gap:6px}
  .trae-icon-btn{background:transparent;border:none;color:var(--fg-dim);cursor:pointer;padding:6px 10px;border-radius:var(--radius-sm);transition:var(--transition);font-size:12px;font-family:var(--font);font-weight:500;display:flex;align-items:center;gap:5px;white-space:nowrap;position:relative}
  .trae-icon-btn:hover{background:var(--bg-hover);color:var(--fg)}

  /* ===== Body ===== */
  .trae-body{display:flex;flex:1;overflow:hidden}

  /* ===== Sidebar ===== */
  .trae-sidebar{width:var(--sidebar-w);background:var(--bg2);border-right:1px solid var(--border);display:flex;flex-direction:column;flex-shrink:0;overflow:hidden}
  .trae-sidebar-nav{flex:1;overflow-y:auto;padding:8px 10px}
  .trae-sidebar-group{padding:4px 0}
  .trae-sidebar-label{font-size:10px;color:var(--muted);font-weight:700;text-transform:uppercase;letter-spacing:.8px;padding:10px 10px 6px}
  .trae-sidebar-item{display:flex;align-items:center;gap:10px;padding:8px 10px;cursor:pointer;font-size:12px;color:var(--fg-dim);transition:var(--transition);border-radius:var(--radius-sm);white-space:nowrap;font-weight:500}
  .trae-sidebar-item:hover{background:var(--bg-hover);color:var(--fg)}
  .trae-sidebar-item.active{background:var(--accent-soft);color:var(--accent);font-weight:600}
  .trae-sidebar-item .icon{font-size:15px;width:20px;text-align:center;flex-shrink:0}
  .trae-sidebar-item .badge{margin-left:auto;background:var(--danger);color:#fff;border-radius:10px;font-size:9px;padding:1px 6px;min-width:14px;text-align:center;font-weight:700;flex-shrink:0}

  /* ===== User Card (bottom-left) ===== */
  .trae-user{padding:12px;border-top:1px solid var(--border);display:flex;align-items:center;gap:10px;cursor:pointer;transition:var(--transition);background:var(--bg-soft)}
  .trae-user:hover{background:var(--bg-hover)}
  .trae-user-avatar{width:34px;height:34px;border-radius:var(--radius-sm);background:linear-gradient(135deg,var(--accent-soft),var(--accent));display:flex;align-items:center;justify-content:center;font-size:17px;flex-shrink:0;overflow:hidden;border:1px solid var(--border)}
  .trae-user-avatar img{width:100%;height:100%;object-fit:cover}
  .trae-user-info{flex:1;min-width:0}
  .trae-user-name{font-size:12px;font-weight:600;color:var(--fg);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .trae-user-uid{font-size:10px;color:var(--muted);font-family:var(--mono)}
  .trae-user-actions{display:flex;flex-direction:column;gap:4px;align-items:flex-end}
  .trae-user-btn{background:transparent;border:none;color:var(--muted);cursor:pointer;font-size:15px;padding:4px;transition:var(--transition);border-radius:var(--radius-xs)}
  .trae-user-btn:hover{color:var(--danger);background:var(--danger-soft)}

  /* ===== B站 Theme Toggle ===== */
  .theme-toggle{position:relative;width:56px;height:28px;border-radius:28px;cursor:pointer;border:none;outline:none;transition:background .4s cubic-bezier(.4,0,.2,1);overflow:hidden;padding:0}
  .theme-toggle.light{background:linear-gradient(135deg,#87ceeb,#e0f6ff)}
  .theme-toggle.dark{background:linear-gradient(135deg,#1a1a2e,#16213e)}
  .toggle-circle{position:absolute;top:3px;left:3px;width:22px;height:22px;border-radius:50%;transition:all .4s cubic-bezier(.68,-0.55,.27,1.55);display:flex;align-items:center;justify-content:center;font-size:13px;line-height:1;z-index:2}
  .theme-toggle.light .toggle-circle{left:3px;background:#fff;box-shadow:0 0 8px rgba(255,200,0,.6),0 2px 4px rgba(0,0,0,.15);transform:rotate(0deg)}
  .theme-toggle.dark .toggle-circle{left:31px;background:#c8c8c8;box-shadow:0 0 10px rgba(255,255,255,.2),0 2px 4px rgba(0,0,0,.3);transform:rotate(360deg)}
  .toggle-circle .sun-icon{display:block}
  .toggle-circle .moon-icon{display:none}
  .theme-toggle.dark .toggle-circle .sun-icon{display:none}
  .theme-toggle.dark .toggle-circle .moon-icon{display:block}
  .toggle-stars{position:absolute;inset:0;opacity:0;transition:opacity .4s}
  .theme-toggle.dark .toggle-stars{opacity:1}
  .toggle-stars span{position:absolute;background:#fff;border-radius:50%;animation:twinkle 2s infinite ease-in-out}
  .toggle-stars span:nth-child(1){width:2px;height:2px;top:5px;right:10px;animation-delay:0s}
  .toggle-stars span:nth-child(2){width:1.5px;height:1.5px;top:10px;right:18px;animation-delay:.5s}
  .toggle-stars span:nth-child(3){width:2px;height:2px;top:18px;right:8px;animation-delay:1s}
  .toggle-stars span:nth-child(4){width:1px;height:1px;top:8px;right:24px;animation-delay:1.5s}
  .toggle-stars span:nth-child(5){width:1.5px;height:1.5px;top:20px;right:22px;animation-delay:.3s}
  @keyframes twinkle{0%,100%{opacity:.2;transform:scale(.8)}50%{opacity:1;transform:scale(1.2)}}
  .toggle-clouds{position:absolute;inset:0;opacity:1;transition:opacity .4s}
  .theme-toggle.dark .toggle-clouds{opacity:0}
  .toggle-clouds span{position:absolute;background:rgba(255,255,255,.7);border-radius:50%;filter:blur(1px)}
  .toggle-clouds span:nth-child(1){width:14px;height:6px;top:7px;right:6px}
  .toggle-clouds span:nth-child(2){width:10px;height:5px;top:16px;right:14px}

  /* ===== Content Area ===== */
  .trae-content{flex:1;overflow-y:auto;padding:24px;display:flex;flex-direction:column;gap:16px}

  /* ===== Card / Box ===== */
  .term-box{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);padding:20px;margin-bottom:16px;position:relative;box-shadow:var(--shadow)}
  .term-box::before{content:attr(data-title);position:absolute;top:-10px;left:16px;background:var(--accent);color:#fff;padding:4px 12px;font-size:10px;border-radius:var(--radius-xs);letter-spacing:.4px;font-weight:700;text-transform:uppercase;box-shadow:var(--shadow-md)}

  /* ===== Login ===== */
  #loginScreen{align-items:center;justify-content:center;background:linear-gradient(135deg,#6366f1 0%,#8b5cf6 50%,#a855f7 100%)}
  .login-wrap{max-width:400px;width:100%}
  .login-card{background:var(--bg2);backdrop-filter:blur(20px);border-radius:24px;box-shadow:var(--shadow-xl);padding:40px 36px;border:1px solid var(--border)}
  .login-logo{text-align:center;margin-bottom:28px}
  .login-logo .logo-icon{width:60px;height:60px;background:linear-gradient(135deg,var(--accent),var(--accent-dark));border-radius:18px;display:inline-flex;align-items:center;justify-content:center;font-size:30px;color:#fff;box-shadow:var(--shadow-lg);margin-bottom:14px}
  .login-logo h1{font-size:22px;font-weight:700;color:var(--fg);letter-spacing:-.6px}
  .login-logo p{font-size:12px;color:var(--fg-light);margin-top:6px}
  .prompt-line{margin:14px 0;display:flex;align-items:center;gap:10px;flex-wrap:wrap}
  .prompt{color:var(--accent-dark);white-space:nowrap;font-weight:600;font-size:13px}
  .prompt-line input{background:var(--bg-soft);border:1.5px solid var(--border);border-radius:var(--radius-sm);color:var(--fg);font-family:var(--font);font-size:14px;padding:11px 14px;outline:none;flex:1;min-width:120px;transition:var(--transition)}
  .prompt-line input:focus{border-color:var(--accent);background:var(--bg2);box-shadow:0 0 0 3px var(--accent-soft)}
  .prompt-line input::placeholder{color:var(--muted)}
  .login-btn{background:linear-gradient(135deg,var(--accent),var(--accent-dark));color:#fff;border:none;border-radius:var(--radius-sm);font-family:var(--font);font-size:14px;padding:12px 24px;cursor:pointer;margin-top:16px;transition:var(--transition);font-weight:600;width:100%;box-shadow:var(--shadow-md)}
  .login-btn:hover{transform:translateY(-1px);box-shadow:var(--shadow-lg)}
  .login-btn:active{transform:translateY(0)}
  .error-msg{color:var(--danger);font-size:12px;margin-top:10px;min-height:16px}
  .hint{color:var(--fg-light);font-size:11px;margin-top:16px;text-align:center}

  /* ===== Buttons ===== */
  .nav-btn{background:var(--bg2);border:1px solid var(--border);color:var(--fg-dim);font-family:var(--font);font-size:12px;padding:7px 14px;cursor:pointer;transition:var(--transition);border-radius:var(--radius-sm);font-weight:500;white-space:nowrap;display:inline-flex;align-items:center;gap:5px}
  .nav-btn:hover{border-color:var(--border-strong);color:var(--fg);background:var(--bg-hover)}
  .nav-btn.active{background:var(--accent);color:#fff;border-color:var(--accent);box-shadow:var(--shadow-md)}
  .nav-btn.danger{color:var(--danger);border-color:var(--danger)}
  .nav-btn.danger:hover{background:var(--danger);color:#fff}
  .conn-status{font-size:11px;font-weight:600;display:flex;align-items:center;gap:4px}
  .conn-status.online{color:var(--success)}
  .conn-status:not(.online){color:var(--danger)}

  /* ===== Panels ===== */
  .panel{display:none;flex:1;flex-direction:column;gap:16px}
  .panel.active{display:flex;animation:panelIn .25s ease}
  @keyframes panelIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:translateY(0)}}
  .panel-title{color:var(--fg);font-size:15px;font-weight:700;display:flex;align-items:center;gap:8px;letter-spacing:-.3px}
  .panel-title::before{content:'';width:3px;height:16px;background:var(--accent);border-radius:2px;display:inline-block}

  /* ===== Lab Map ===== */
  .lab-map{display:grid;grid-template-columns:repeat(4,1fr);grid-template-rows:auto 32px auto;gap:12px}
  .lab-corridor{grid-column:1/-1;background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius-sm);display:flex;align-items:center;justify-content:center;font-size:12px;color:var(--muted);letter-spacing:6px;box-shadow:var(--shadow-xs);font-weight:500}
  .room-term{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);position:relative;padding:18px;box-shadow:var(--shadow);transition:var(--transition)}
  .room-term:hover{box-shadow:var(--shadow-md);border-color:var(--accent-light);transform:translateY(-1px)}
  .room-term .rt-header{display:flex;justify-content:space-between;align-items:center;padding-bottom:12px;margin-bottom:12px;border-bottom:1px solid var(--border-light)}
  .room-term .rt-name{font-size:14px;font-weight:700;color:var(--fg);letter-spacing:-.2px}
  .room-term .rt-chip{font-size:10px;color:var(--accent);background:var(--accent-soft);border:none;padding:3px 9px;border-radius:var(--radius-xs);font-weight:700;letter-spacing:.3px}
  .room-door-marker{position:absolute;top:-10px;left:14px;background:var(--warning);color:#fff;font-size:10px;padding:3px 10px;z-index:2;letter-spacing:.4px;white-space:nowrap;border-radius:var(--radius-xs);border:none;font-weight:700;box-shadow:var(--shadow-md)}
  .sensor-line{font-size:12px;padding:5px 0;display:flex;justify-content:space-between;gap:8px}
  .sensor-line .s-label{color:var(--fg-light);white-space:nowrap}
  .sensor-line .s-val{color:var(--fg);font-weight:600;text-align:right;white-space:nowrap;font-family:var(--mono);font-size:11px}
  .sensor-line .s-val.warn{color:var(--warning)}
  .sensor-line .s-val.danger{color:var(--danger)}
  .light-bar{margin-top:14px;padding-top:12px;border-top:1px solid var(--border-light);display:flex;gap:8px;flex-wrap:wrap;align-items:center}
  .light-tag{font-size:11px;padding:4px 10px;border:1px solid var(--border);color:var(--fg-light);border-radius:20px;background:var(--bg-soft);transition:var(--transition);font-weight:500}
  .light-tag.on{border-color:var(--success);color:var(--success);background:var(--success-soft);font-weight:600}
  .light-btn-term{background:var(--bg-soft);border:1px solid var(--border);color:var(--fg-dim);font-family:var(--font);font-size:11px;padding:5px 12px;cursor:pointer;transition:var(--transition);border-radius:var(--radius-xs);font-weight:500}
  .light-btn-term:hover{border-color:var(--accent-light);color:var(--accent);background:var(--accent-soft)}
  .light-btn-term.on{border-color:var(--success);color:var(--success);background:var(--success-soft);font-weight:600}
  .light-readonly{font-size:11px;padding:4px 10px;border:1px dashed var(--border);color:var(--muted);border-radius:20px;background:var(--bg-soft);font-style:italic;white-space:nowrap}

  /* ===== Tables ===== */
  .term-table{width:100%;border-collapse:separate;border-spacing:0;font-size:12px;border-radius:var(--radius-sm);overflow:hidden;box-shadow:var(--shadow);border:1px solid var(--border)}
  .term-table th,.term-table td{border:none;border-bottom:1px solid var(--border-light);padding:11px 14px;text-align:left}
  .term-table th{background:var(--bg-soft);color:var(--fg-dim);font-weight:600;white-space:nowrap;font-size:11px;letter-spacing:.4px;text-transform:uppercase}
  .term-table tr:last-child td{border-bottom:none}
  .term-table tr:hover td{background:var(--bg-hover)}
  .term-btn-sm{background:var(--bg-soft);border:1px solid var(--border);color:var(--fg-dim);font-family:var(--font);font-size:11px;padding:4px 10px;cursor:pointer;transition:var(--transition);border-radius:var(--radius-xs);font-weight:600;white-space:nowrap;margin-right:4px}
  .term-btn-sm:hover{background:var(--accent-soft);color:var(--accent);border-color:var(--accent-light)}
  .term-btn-sm.danger{color:var(--danger)}
  .term-btn-sm.danger:hover{background:var(--danger-soft);color:var(--danger);border-color:var(--danger)}
  .term-btn-sm.info{color:var(--info)}
  .term-btn-sm.info:hover{background:var(--info-soft);color:var(--info);border-color:var(--info)}

  /* ===== Forms ===== */
  .term-form{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:14px;margin:14px 0;padding:18px;background:var(--bg-soft);border-radius:var(--radius-sm);border:1px solid var(--border-light)}
  .term-form label{font-size:10px;color:var(--fg-light);display:block;margin-bottom:6px;font-weight:700;letter-spacing:.4px;text-transform:uppercase}
  .term-form input,.term-form select{background:var(--bg2);border:1.5px solid var(--border);color:var(--fg);font-family:var(--font);font-size:12px;padding:9px 12px;outline:none;width:100%;border-radius:var(--radius-xs);transition:var(--transition)}
  .term-form input:focus,.term-form select:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-soft)}

  /* ===== Tabs ===== */
  .term-tabs{display:flex;gap:2px;margin-bottom:16px;border-bottom:1px solid var(--border);padding-bottom:0}
  .term-tab{background:transparent;border:none;color:var(--fg-light);font-family:var(--font);font-size:13px;padding:8px 16px;cursor:pointer;border-bottom:2px solid transparent;margin-bottom:-1px;transition:var(--transition);font-weight:500}
  .term-tab:hover{color:var(--fg-dim)}
  .term-tab.active{color:var(--accent);border-bottom-color:var(--accent);font-weight:600}

  /* ===== Toast ===== */
  .term-toast{position:fixed;bottom:24px;right:24px;background:var(--fg);color:var(--bg);border:none;padding:12px 22px;font-size:13px;transform:translateX(150%);transition:transform .35s cubic-bezier(.4,0,.2,1);z-index:10000;border-radius:var(--radius-sm);box-shadow:var(--shadow-xl);font-weight:600}
  .term-toast.show{transform:translateX(0)}

  /* ===== Admin visibility ===== */
  .admin-only{display:none}
  body.is-admin .admin-only{display:inline-flex}
  body.is-admin .admin-only.block{display:block}
  body.is-admin .admin-only.flex{display:flex}
  body.is-admin .trae-cat.admin-only{display:inline-flex}

  /* ===== Inventory Cards ===== */
  .card-stack{display:grid;grid-template-columns:repeat(auto-fill,minmax(230px,1fr));gap:14px;margin-bottom:16px}
  .inv-card{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);padding:18px;cursor:pointer;transition:var(--transition);box-shadow:var(--shadow);position:relative}
  .inv-card:hover{box-shadow:var(--shadow-lg);transform:translateY(-2px);border-color:var(--accent-light)}
  .inv-card .card-icon{font-size:28px;margin-bottom:10px}
  .inv-card .card-name{font-size:14px;font-weight:700;color:var(--fg);margin-bottom:6px;letter-spacing:-.2px}
  .inv-card .card-count{font-size:32px;font-weight:800;color:var(--accent);margin-bottom:10px;line-height:1;letter-spacing:-1px}
  .inv-card .card-count span{font-size:12px;font-weight:400;color:var(--muted)}
  .inv-card .card-actions{display:flex;gap:6px;align-items:center}
  .inv-card .qty-btn{width:34px;height:34px;border-radius:var(--radius-sm);border:1px solid var(--border);background:var(--bg-soft);color:var(--fg-dim);font-size:18px;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:var(--transition)}
  .inv-card .qty-btn:hover{background:var(--accent);color:#fff;border-color:var(--accent);transform:scale(1.05)}
  .inv-card .qty-btn.minus:hover{background:var(--danger);border-color:var(--danger)}
  .inv-card .qty-display{font-size:14px;font-weight:600;color:var(--fg);min-width:38px;max-width:60px;text-align:center;background:var(--bg2);border:1.5px solid var(--border);border-radius:var(--radius-xs);padding:5px 2px;outline:none;font-family:var(--font);transition:var(--transition)}
  .inv-card .qty-display:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--accent-soft)}
  .inv-card .card-detail-hint{font-size:10px;color:var(--muted);margin-top:8px}
  .inv-card .card-del-btn{position:absolute;top:10px;right:10px;background:none;border:none;color:var(--muted);font-size:18px;cursor:pointer;padding:2px 6px;line-height:1;border-radius:var(--radius-xs);transition:var(--transition)}
  .inv-card .card-del-btn:hover{color:var(--danger);background:var(--danger-soft)}

  /* ===== Detail Panel ===== */
  .detail-panel{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow);overflow:hidden;transition:max-height .35s cubic-bezier(.4,0,.2,1),opacity .3s;max-height:0;opacity:0;margin-bottom:0}
  .detail-panel.open{max-height:2000px;opacity:1;margin-bottom:16px}
  .detail-panel .detail-header{display:flex;align-items:center;justify-content:space-between;padding:14px 18px;background:var(--bg-soft);cursor:pointer;border-bottom:1px solid var(--border-light)}
  .detail-panel .detail-header h3{font-size:14px;color:var(--fg);font-weight:600;margin:0}
  .detail-panel .detail-close{background:none;border:none;color:var(--fg-light);font-size:20px;cursor:pointer;padding:4px 8px;border-radius:var(--radius-xs);transition:var(--transition);line-height:1}
  .detail-panel .detail-close:hover{color:var(--danger);background:var(--danger-soft)}
  .detail-panel .detail-body{padding:0}
  .detail-panel .detail-body table{margin:0}

  /* ===== Modal ===== */
  .modal-overlay{position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(15,23,42,.45);backdrop-filter:blur(6px);z-index:9999;justify-content:center;align-items:center;animation:modalIn .2s ease}
  @keyframes modalIn{from{opacity:0}to{opacity:1}}
  .modal-card{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);padding:28px;max-width:480px;width:90%;box-shadow:var(--shadow-xl);animation:cardIn .25s cubic-bezier(.4,0,.2,1)}
  @keyframes cardIn{from{opacity:0;transform:scale(.96) translateY(8px)}to{opacity:1;transform:scale(1) translateY(0)}}
  .modal-title{font-size:17px;font-weight:700;color:var(--fg);margin-bottom:20px;display:flex;align-items:center;gap:8px;letter-spacing:-.3px}
  .modal-title::before{content:'';width:4px;height:18px;background:var(--accent);border-radius:2px}
  .modal-field{margin-bottom:14px}
  .modal-field label{font-size:11px;color:var(--fg-light);display:block;margin-bottom:6px;font-weight:700;letter-spacing:.3px;text-transform:uppercase}
  .modal-field input,.modal-field select,.modal-field textarea{width:100%;background:var(--bg-soft);border:1.5px solid var(--border);border-radius:var(--radius-xs);padding:10px 12px;color:var(--fg);font-size:13px;font-family:var(--font);outline:none;transition:var(--transition)}
  .modal-field input:focus,.modal-field select:focus,.modal-field textarea:focus{border-color:var(--accent);background:var(--bg2);box-shadow:0 0 0 3px var(--accent-soft)}
  .modal-field textarea{resize:vertical}
  .modal-actions{display:flex;gap:8px;justify-content:flex-end;margin-top:20px}

  /* ===== Request Items ===== */
  .req-item{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius-sm);padding:16px;margin-bottom:10px;transition:var(--transition)}
  .req-item:hover{border-color:var(--border-strong);box-shadow:var(--shadow)}

  /* ===== Profile ===== */
  .profile-card{background:var(--bg2);border:1px solid var(--border);border-radius:var(--radius);padding:28px;box-shadow:var(--shadow)}
  .profile-header{display:flex;align-items:center;gap:20px;margin-bottom:24px;padding-bottom:20px;border-bottom:1px solid var(--border-light)}
  .profile-avatar{width:72px;height:72px;border-radius:var(--radius-md);background:linear-gradient(135deg,var(--accent-soft),var(--accent));display:flex;align-items:center;justify-content:center;font-size:36px;box-shadow:var(--shadow-md)}
  .profile-info h2{font-size:20px;font-weight:700;color:var(--fg);margin-bottom:4px;letter-spacing:-.4px}
  .profile-info .uid{font-size:13px;color:var(--accent);font-weight:600;background:var(--accent-soft);padding:3px 10px;border-radius:var(--radius-xs);display:inline-block;font-family:var(--mono)}
  .profile-info .role{font-size:12px;color:var(--fg-light);margin-top:6px}
  .profile-row{display:flex;justify-content:space-between;align-items:center;padding:12px 0;border-bottom:1px solid var(--border-light)}
  .profile-row:last-child{border-bottom:none}
  .profile-row .label{color:var(--fg-light);font-size:13px;font-weight:500}
  .profile-row .value{color:var(--fg);font-size:14px;font-weight:600}
  .avatar-picker{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px}
  .avatar-option{width:40px;height:40px;border-radius:var(--radius-sm);border:2px solid var(--border);background:var(--bg-soft);display:flex;align-items:center;justify-content:center;font-size:22px;cursor:pointer;transition:var(--transition)}
  .avatar-option:hover{border-color:var(--accent-light);transform:scale(1.1)}
  .avatar-option.selected{border-color:var(--accent);background:var(--accent-soft);box-shadow:0 0 0 3px var(--accent-soft)}

  /* ===== Contacts / Chat ===== */
  .contact-item{display:flex;align-items:center;gap:10px;padding:10px 12px;cursor:pointer;transition:var(--transition);border-radius:var(--radius-sm);border-left:3px solid transparent}
  .contact-item:hover{background:var(--bg-hover)}
  .contact-item.active{background:var(--accent-soft);border-left-color:var(--accent)}
  .contact-item .c-avatar{width:36px;height:36px;border-radius:var(--radius-sm);background:var(--accent-soft);display:flex;align-items:center;justify-content:center;font-size:18px;flex-shrink:0;overflow:hidden}
  .contact-item .c-avatar img{width:100%;height:100%;object-fit:cover}
  .contact-item .c-info{flex:1;min-width:0}
  .contact-item .c-name{font-size:13px;font-weight:600;color:var(--fg);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .contact-item .c-last{font-size:11px;color:var(--muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
  .contact-item .c-badge{background:var(--danger);color:#fff;border-radius:10px;font-size:10px;padding:1px 6px;min-width:14px;text-align:center;font-weight:700;flex-shrink:0}
  .chat-bubble{max-width:70%;padding:10px 14px;border-radius:14px;font-size:13px;line-height:1.5;word-break:break-word}
  .chat-bubble.me{background:var(--accent);color:#fff;border-bottom-right-radius:4px;align-self:flex-end}
  .chat-bubble.other{background:var(--bg2);border:1px solid var(--border);color:var(--fg);border-bottom-left-radius:4px;align-self:flex-start}
  .chat-bubble.broadcast{background:var(--warning-soft);border:1px solid var(--warning);color:var(--fg);align-self:center;border-radius:10px;text-align:center;max-width:85%}
  .chat-time{font-size:10px;color:var(--muted);margin-top:2px}
  .friend-req-item{display:flex;align-items:center;gap:8px;padding:8px 12px;border-bottom:1px solid var(--border-light);font-size:12px}
  .friend-req-item .r-avatar{font-size:16px}
  .friend-req-item .r-name{flex:1;color:var(--fg);font-weight:500}

  /* ===== Responsive ===== */
  @media(max-width:768px){
    .trae-topbar{flex-direction:column;align-items:flex-start;height:auto;padding:8px 12px}
    .trae-brand{margin-right:0;margin-bottom:4px}
    .trae-topbar-right{flex-wrap:wrap;gap:4px}
    .trae-sidebar{width:56px}
    .trae-sidebar-item span:not(.icon):not(.badge){display:none}
    .trae-sidebar-label{display:none}
    .trae-user-info,.trae-user-actions{display:none}
    .trae-content{padding:12px}
    .term-form{grid-template-columns:1fr}
    .lab-map{grid-template-columns:1fr}
    .card-stack{grid-template-columns:1fr}
  }
</style>