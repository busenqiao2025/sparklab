# SparkMinds Lab 更新日志

## v2.6.3 — 实验室监控 · 平面图改卡片流 — 2026-09-25

#### 🗺️ → 🃏 布局重做：不要地图，直接卡片
- **移除全部平面图元素**：走廊横条、🚪大门入口标签、3D 打印区跨两列 —— 全部删除，房间按顺序平铺成整齐的卡片网格
- **自适应卡片网格**：桌面按宽度自动 3～4 列（`auto-fill + minmax(300px,1fr)`），平板 2 列，手机单列，任何屏宽都不再有碎列
- **每张卡片信息更聚焦**：
  - 头部 = 状态点 + 房间名 + 设备型号徽标（ESP32C3 / ESP32）
  - 传感器 6 项改为 **2×3 紧凑数据格**（温度 / 湿度 / 光照 / 空气质量 / TVOC / eCO2），一眼扫完
  - 底部保留灯光状态与开关（管理员可远程控制）
- **新增设备在线状态**：卡片头部实时显示「实时 / 等待数据 / N 分钟前 / 离线」——超过 5 分钟无数据变黄（陈旧），超过 30 分钟变灰（离线），每 15 秒自动刷新
- **标签接入 i18n**：传感器标签原先硬编码中文，现走翻译字典（简中 / 繁中 / 英文）；切换语言时实验室面板自动重绘，已收到的数值不丢失（内置数据回放缓存）
- 顺手清理了废弃的平面图文案（走廊 / 大门）与隐藏彩蛋时间线的版本错标（v2.6.1 / v2.6.2 条目已补齐）

#### 验证
- `verify-v263.js` **27 项断言全过**（卡片结构 / 网格列数 / 状态文案 / 数据缓存回放 / 英文重绘）
- 回归：桌面 `verify-v27.js` 32/32、手机 `verify-v262.js` 29/29，重复 key 扫描无新增

## v2.6.2 — 手机端适配 · 抽屉导航 · 聊天视图 — 2026-09-25

#### 📱 布局重构：手机真正可读可操作
- **顶栏压缩为单行**（48px）：隐藏在线状态文字、模拟开关、站内信入口，只留 ☰ / 品牌+版本 / 🔔 / 明暗切换 / 语言，不再挤成两行占半屏
- **侧边栏改为抽屉式导航**：手机上点 ☰ 从左侧滑出（带遮罩），恢复**完整文字标签**、用户信息、退出按钮、空间切换器；点具体面板或遮罩自动收起，从手机尺寸切回桌面自动复位
- **分类入口移进抽屉**：实验室监控 / 库存管理 / 用户中心 / 管理后台变为抽屉顶部的分类按钮（同步高亮），顶栏不再拥挤；桌面端完全不受影响
- **汉堡按钮状态随动**：抽屉打开显示 ✕，关闭显示 ☰

#### 🔧 内容区修复（之前几乎不可读的重灾区）
- **实验室监控**：修复 3D 打印卡片的 `span 2` 在单列网格里**撑出隐式列**、把整个房间地图挤成两条不等宽碎列导致文字竖排重叠的问题——现在单列全宽展示，传感器行清晰可读
- **站内信**：重构为**列表 / 聊天二选一**视图——进入显示联系人列表，点联系人进入全宽聊天（带 ← 返回按钮），彻底解决聊天区被挤出屏幕的问题
- **仪表盘**：KPI 指标卡改两列排布，不再一张卡占一整屏
- **表格**：库存流水 / 用户管理 / 交易记录等所有表格在手机上**整块横向滚动**，不再压碎内容
- **弹窗**：模态框宽度自适应到 94vw、内部可滚动；通知下拉、更新日志抽屉同样收窄
- 内容区留白收紧，论坛统计两列等既有适配全部保留

#### 其他
- `switchCategory` 高亮逻辑改为同步**顶栏 + 抽屉**两处分类按钮
- 版本号 v2.6.1 → v2.6.2（修订号：单点专项优化；标题栏、顶栏徽标、关于页、导出报告页脚、更新日志抽屉、控制台彩蛋、彩蛋时间线新增 v2.6.2 条目 📱）
- 新增 1 组三语文案（mobile_back）

#### 验证
- `verify-v262.js`（390×844 手机视口）**29 项断言全过**：顶栏 48px 单行 / 三项次要入口隐藏、KPI 两列、无横向溢出、抽屉收起定位 / 克隆分类 4 个 / 开关与按钮态 / 导航文字恢复 / 分类切换不收起 / 面板点击自动收起 / 遮罩关闭、实验室单列 + 跨列修复、站内信列表↔聊天切换与返回按钮、桌面端 1440px 下侧边栏与顶栏分类不受影响；零控制台报错
- `verify-v27.js` 桌面端回归 **32 项断言全过**（主题样式功能无回归）
- 重复 i18n key 扫描：仍为原有 6 组无害重复（×3 语言），无新增冲突

## v2.6.1 — 主题样式 · 六套组合预设 — 2026-09-25

#### 🎨 一键套用主题样式
- 设置页新增**「主题样式」**卡片，内置 **6 套组合预设**，点一下同时套用**主色 + 圆角 + 界面密度 + 明暗模式 + 壁纸**五项外观设置，不用再一项项调

| 预设 | 主色 | 圆角 | 密度 | 模式 | 壁纸 |
|---|---|---|---|---|---|
| 经典蓝 Classic | `#6366f1` 靛蓝 | 14px | 舒适 | 浅色 | 无 |
| 极简灰 Minimal | `#64748b` 石板灰 | 4px | 紧凑 | 浅色 | 无 |
| 活力橙 Vibrant | `#f97316` 橙 | 20px | 舒适 | 浅色 | 落日 |
| 森林绿 Forest | `#10b981` 翠绿 | 12px | 舒适 | 浅色 | 森林 |
| 暗夜科技 Midnight Tech | `#06b6d4` 青 | 8px | 紧凑 | **深色** | 星云 |
| 樱花粉 Sakura | `#ec4899` 粉 | 18px | 舒适 | 浅色 | 蜜桃 |

#### 🧩 预设卡片
- 每张卡片带**实时预览**：三个色块展示主色 / 浅色 / 深色层次 + 圆角形状示意，所见即所得
- 当前生效的预设**高亮显示**；卡片下方常驻「当前样式：xxx」提示行
- 卡片支持**键盘操作**（Enter / 空格切换，Tab 可聚焦），移动端自动两列排布

#### ✏️ 自定义状态识别
- 手动改动任一外观项（主色、圆角、密度、明暗模式、壁纸）后，样式自动切换为**「自定义」**，预设卡片全部取消高亮，提示行显示「当前样式：自定义」
- 套用预设时内部加 `tsApplying` 标记，避免批量写入设置被误判成自定义
- 手动改主色时，上方「主色」选择区的选中态与取色器**同步更新**，两处不会打架

#### 其他
- 样式选择持久保存（`lab_theme_style`），刷新后自动还原整套外观
- 新增 12 组主题样式文案，简中 / 繁中 / 英文三语补齐；切换语言后设置页即时重绘
- 版本号 v2.6 → **v2.6.1**（本次为小迭代，按「只有大更新才升大版本号」的约定走修订号；涉及标题栏、顶栏徽标、关于页、导出报告页脚、更新日志抽屉、控制台彩蛋、彩蛋时间线新增 v2.6.1 条目）

#### 验证
- `verify-v27.js` **32 项断言全过**（改号后重跑仍全过）：6 张预设卡片与名称/预览渲染、默认选中态、当前样式提示行、套用「暗夜科技」（主色 #06b6d4 / 圆角 8px / 密度紧凑 / data-theme=dark / 壁纸星云 / 存档写入 / 卡片高亮 / toast 提示 / 主色选择区同步）、套用「极简灰」、手动改主色转自定义、手动改圆角转自定义、刷新后「樱花粉」整套外观复原、英文界面名称与标签、零控制台报错
- 重复 i18n key 扫描：仍为 6 个无害重复（与 v2.6 相同），无新增冲突

## v2.6 — 论坛改版 · 搜索排序 · 富文本 — 2026-09-25

#### 🏛 论坛首页全面升级
- 新增**论坛统计条**：帖子数 / 总浏览 / 总点赞 / 总评论，四个指标一目了然（全部由真实帖子数据汇总）
- 新增**搜索框**：按标题、正文、作者即时搜索（180ms 防抖），可搜「风扇」也能搜作者「小红」；无结果时提供「清除筛选」一键恢复
- 新增**排序切换**：最新 / 最热（点赞×2 + 评论×3 + 浏览×0.5 综合热度）/ 浏览最多
- 分类按钮带**实时计数**徽标（全部 5 · 技术交流 1 …），发帖后自动更新
- 顶部新增 **↻ 刷新**按钮，与发帖按钮并列

#### 🃏 帖子卡片重绘
- 每张卡片带**作者头像**（昵称首字符 + 按昵称生成的固定配色）、作者名、**相对时间**（刚刚 / n 分钟前 / n 小时前 / n 天前，30 天以上显示日期）
- 分类彩色标签、**🔥 热门徽章**（热度达到最高分的 60% 以上自动点亮）
- 摘要两行截断不撑破版面；卡片**直接放点赞按钮**，不用进详情就能点赞，点赞时有弹跳动画
- 空状态分两种：完全没帖子引导「去发帖」，有筛选无结果引导「清除筛选」

#### 📖 帖子详情美化
- 顶部**作者信息卡**：头像 + 昵称 + **楼主**标记 + 发布时间，右侧分类标签
- 正文支持**轻量富文本**：`**粗体**`、`` `行内代码` ``、`- 列表`、`[文字](链接)` 与裸链接自动识别（安全转义后渲染，链接新窗口打开）
- 评论区每条评论带头像、相对时间，**楼主评论有专属标记**；发表评论成功后自动刷新且输入框自动聚焦
- 操作行同时显示浏览数、评论数，删除失败时给出具体原因提示

#### 🛡 安全修复（重要）
- 论坛标题 / 正文 / 评论此前**未做 HTML 转义**，存在被注入脚本的风险；v2.6 全部先转义再渲染，`<script>`、`<img onerror>` 等一律按纯文本显示（验证脚本实测注入不生效）
- **删除帖子增加后端权限校验**：此前任何人调用接口可删除任意帖子，现在仅**作者本人或管理员**可删（前端请求附带 uid，后端校验角色）

#### ✍️ 发帖体验
- 新增**草稿自动保存**：标题/内容每敲一次自动存 `lab_forum_draft`，误关弹窗不丢内容；重开自动恢复并显示提示条，可一键「丢弃」
- 标题/内容**字数统计**（60 / 2000，超出变红并在发布前拦截）
- 新增**格式工具栏**：粗体 B、代码 `</>`、列表、链接 🔗 四个按钮，自动包裹选中文字

#### 其他
- 版本号 v2.5.1 → v2.6（标题栏、顶栏徽标、关于页、导出报告页脚、更新日志抽屉、控制台彩蛋、彩蛋时间线新增 v2.6 条目）
- 新增 38 组论坛文案，简中 / 繁中 / 英文三语补齐；切换语言后论坛即时重绘
- 移动端适配：统计条两列、工具栏纵向排列、头像缩小

#### 验证
- `verify-v26.js` **57 项断言全过**：统计与分类计数、搜索与空状态、三种排序、卡片点赞/取消赞、详情富文本渲染（粗体/列表/链接/行内代码）、XSS 注入不生效、评论发送、草稿保存/恢复/清除、字数统计、发布流程、删除请求携带 uid、英文界面切换；零控制台报错
- 重复 i18n key 扫描：仅存 6 个无害重复（与 v2.5.1 相同），无新增冲突

## v2.5.1 — 侧边栏折叠 · 动画开关 · 最近使用 — 2026-09-25

#### 📐 侧边栏折叠
- 顶栏最左侧新增 **☰ 按钮**（快捷键 **Ctrl/⌘ + B**），一键把侧边栏收成 60px 纯图标栏，内容区更宽敞
- 折叠状态自动记住（`lab_sidebar`），刷新后保持；再次点击或按 Ctrl+B 展开
- 折叠时底部用户信息只保留头像，空间切换器（管理员）自动变为纵向排列

#### ✨ 动画效果开关（无障碍）
- 设置 → 「界面密度」卡片下新增**动画效果**：开启 / 关闭
- 关闭后全局禁用过渡与动画（数据来自 `data-motion="off"`），对容易晕动效的用户更友好
- **默认跟随系统**：若操作系统开了「减弱动态效果」（prefers-reduced-motion），应用自动关闭动画；系统设置变化时实时跟随
- 手动选择后以手动为准，选择持久保存（`lab_motion`）

#### 🕘 命令面板「最近使用」
- Ctrl+K 打开命令面板时，**未输入搜索词**会先显示「最近使用」分组（最多 5 个刚访问过的面板，`lab_cmdk_recent`），最常用的功能一步直达
- 输入关键词后自动切换为搜索模式，行为与 v2.5 一致

#### 其他
- 版本号 v2.5 → v2.5.1（标题栏、顶栏徽标、关于页、导出报告页脚、更新日志抽屉、控制台彩蛋）
- 快捷键帮助（`?`）补充 Ctrl/⌘+B；新增文案三语补齐

#### 验证
- `verify-v251.js` 14 项断言全过：折叠/展开与持久化、Ctrl+B、最近使用分组与排序、动效开关与 computed transition 归零、英文文案存活；零控制台报错
- 回归：v2.5 功能 23 项、i18n 词条修复 11 项均全过

## v2.5 — 命令面板 · 界面密度 · 更新日志抽屉 — 2026-09-25

#### ⌘ 命令面板（Ctrl / ⌘ + K）
- 任意页面按 **Ctrl+K**（Mac 为 ⌘+K）唤起全局命令面板，**搜索即达**：支持中文、英文与拼音关键词（如输入“仓库”“settings”“shezhi”均可命中）
- 覆盖全部 15 个功能面板（管理员多 6 个管理项），按分类分组展示，右侧带分类标签
- 内置 6 个**快捷动作**：切换深浅主题、切换语言（简中/繁中/English 一键轮换）、导出数据、快捷键说明、查看更新日志、刷新当前页面
- 完整键盘导航：`↑↓` 选择、`Enter` 执行、`Esc` 关闭；点击遮罩或条目同样可用
- 快捷键帮助（`?`）中新增该条目

#### 📏 界面密度
- 设置页新增「界面密度」：**舒适 / 紧凑** 两档
- 紧凑模式全局收紧面板间距、卡片内边距与侧栏行高，一屏可见更多内容；选择持久保存（`lab_density`），刷新后保持

#### 🎨 外观模式：跟随系统
- 主题设置新增三档：**浅色 / 深色 / 跟随系统**
- 「跟随系统」实时响应操作系统的深浅色变化（`prefers-color-scheme`），无需手动切换；原有的主题动画开关仍然可用，且与三档选择联动
- 兼容旧数据：已有用户的浅色/深色偏好自动迁移为新模式

#### 📜 更新日志抽屉
- **点击顶栏版本号**（v2.5 徽标）即可打开右侧滑出的「更新日志」抽屉，直接查看管理员发布的历史更新（数据来自服务端 changelog，与公告管理同源）
- 有**新的更新日志**时，版本号徽标右上角会出现呼吸红点提示；打开抽屉后自动标记已读
- 双击版本号的彩蛋（版本时间线）保留不冲突，时间线已加入 v2.5 条目

#### 其他
- 全站版本号 v2.4 → v2.5（标题栏、顶栏徽标、关于页、导出报告页脚、控制台彩蛋）
- 新增文案均补齐简中 / 繁中 / English 三语

#### 🐛 修复：i18n 词条 key 冲突（站内信举报按钮/弹窗标题错乱）
- 根因：`report_title` 在同一语言字典中被定义两次（「举报用户」与「SparkMinds Lab 实验报告」），后者覆盖前者 → 站内信聊天的 🚨 举报按钮、举报弹窗标题都错误显示为报告标题；`req_approved` / `req_denied` 同样存在「已批准该申请 / 已批准」双定义。
- 修复：导出报告标题改用新键 `export_report_title`；审批操作的 toast 提示改用 `req_approved_toast` / `req_denied_toast`；`report_title` 恢复为「举报用户」，`req_approved` / `req_denied` 恢复为筛选标签用短文案。三套语言同步修正，并用脚本扫描确认无其他真冲突（其余 6 组重复值相同、无害）。
- 验证：`verify-i18n-fix.js` 11 项断言全过（简中/英文词条、举报弹窗实际标题、导出标题），零控制台报错；v2.5 功能回归 23 项仍全过。

#### 验证
- `verify-v25.js`（Playwright + mock API）23 项断言全部通过、零控制台报错：命令面板开关/过滤（中英文）/回车跳转、密度切换持久化、三档外观模式、更新日志抽屉开关与条目数、英文切换后新文案存活（Compact / System / 英文占位符）

## 公告闪烁根因修复：统一为单一数据源 — 2026-09-25

#### 现象
- 刷新应用时，公告在“老公告”和“新公告”之间来回跳：有时显示几条老公告（新发的那条没了），有时只显示新公告（老的没了）。

#### 根因
- KV 中**同时存在两个数据源**：旧的无后缀 key `announcement`（2 条老公告）与新 key `announcement_A`（1 条新公告）。
- GET 接口为了防止“读不到”，做了**回退读旧 key**的兜底。由于 Cloudflare KV / CDN 的边缘节点同步有延迟，不同网络、不同时刻可能读到不同来源 → 公告列表在两个来源之间反复切换，表现为“一会有、一会没有”。

#### 修复
1. **一次性数据迁移**（脚本 `kv-migrate.mjs`，已在生产执行，并落 `backup-*.json` 备份）：把旧 key 的条目按 id/标题/内容去重合并进 `announcement_A` / `changelog_A`，写成功后删除旧 key。迁移后空间 A 公告 3 条、更新日志 3 条，老数据全部找回。
2. **代码去掉双数据源**：`GET /api/notice` 只读 `<type>_<space>`，不再回退旧 key；`PUT` 也移除旧 key 合并逻辑，保证读写目标唯一，避免已删除条目被再次捡回。
3. 保留：公告接口 `Cache-Control: no-store`、HTML 入口强制 `no-store`（避免 CDN 长缓存让前端更新失效）、前端空数据降级保护。

#### 验证
- 连续 12 次请求 `/api/notice?space=A`，返回**完全一致**（公告 3 条 / 更新日志 3 条），响应头 `Cache-Control: no-store` 已生效（证明新 worker 正在运行）。
- 首页已是最新版本（含 `hadData` 降级逻辑，字节数 471403），不再被 CDN 返回旧页面。
- `space=B` 数据未受影响（仍为 1 条）。

## 公告闪烁修复（前端 + 后端）— 2026-09-25

#### 问题
- 公告接口偶发返回空（Cloudflare KV 最终一致性 / CDN 缓存到空响应），前端 `loadNotice` 当时会把 `noticeData` 立即清空 → 刷新页面时公告“一会消失一会回来”。

#### 修复
- 前端：`loadNotice` 的 fetch 加 `cache:'no-store'`，且接口返回空数组时不覆盖已展示的公告（降级保护），避免空白。
- 后端：`GET /api/notice` 对空间 A 增加空数组兜底（回退读旧 key）；响应加 `Cache-Control: no-store`。
- Worker：静态资源兜底对 HTML 入口强制 `no-store`，避免 CDN 长缓存导致前端更新不生效。

## 系统公告丢失修复（后端 worker.js）— 2026-09-24

#### 问题
- 发布公告（PUT `/api/notice`）只读写带空间后缀的 key（如 `announcement_A`），但**没有像读取接口那样兼容旧的无后缀 key（`announcement`）**。
- 当旧公告存储在旧 key 时，登录页靠 GET 的兜底还能看到它们；可一旦管理员“新增”公告，PUT 读不到旧 key → 列表被重置为仅新公告 → 旧公告“消失”。

#### 修复
- 写入时若当前为空间 A 且旧无后缀 key 存在，将其**独有条目合并**进当前 space key，并按更新时间排序，随后删除旧 key；此后写入始终基于完整列表，不再丢失。
- 该修复在部署 worker 后生效；部署后**任意一次**新增 / 编辑 / 删除公告操作，都会自动把旧公告合并回来（已用本地单测验证：编辑场景旧公告 3/3 回归、新增场景亦不丢）。

#### 验证
- `verify-notice.mjs`：模拟“announcement_A 仅含新公告 + announcement 含 3 条旧公告”的现状，执行 update / add，断言旧公告全部回归且旧 key 被删除；另测无旧 key 的正常路径不丢不重复。全部 PASS。

## 个人主页增强 — 2026-09-24（第二轮追加）

#### 问候与数据概览
- 右栏顶部新增**时间问候**（早上好 / 下午好 / 晚上好 / 夜深了 + 昵称），按时段自动切换，配一句副标题
- 新增 4 项**数据概览**卡片，全部为本地真实计算、不新增任何 API 请求：
  - 资料完整度（昵称 / 自定义头像 / 密码 各占 1/3，带渐变进度条）
  - 我的角色、账号状态（在线绿点）、注册于 X 天（由创建时间推算）

#### 布局与去重
- 内容区宽度上限 600px → 1000px：宽屏下数据卡一行排开、账号信息两列，不再大片留白
- 左栏去掉与右栏重复的「账号名」「注册时间」行，改为 `@账号名` 等宽句柄 + 「注册于」日期一行
- 头像加**在线状态绿点**；封面渐变叠加细点阵纹理，不再是一块纯色

#### 编辑体验
- 「编辑昵称 / 选择头像」合并为「编辑资料」区，昵称输入框右侧实时显示字数（x/20）
- 账号信息下新增**账号安全提示**（密码由管理员统一维护，引导前往设置）
- 补齐简中 / 繁中 / 英文翻译（问候 / 完整度 / 状态 / 天数 / 安全提示等键）

## 个人主页排版 / 审批界面 / 圆角调节 — 2026-09-24

#### 个人主页排版重做
- 原来是单列 600px 窄卡片：头像、信息行、编辑表单全部垂直堆叠，右侧大片留白，信息密度低
- 改为**两栏布局**（左 300px 身份卡 / 右自适应内容区，窄屏自动单列）：
  - 左栏身份卡：渐变封面 + 悬浮头像 + 昵称 + UID / 角色标签，配「保存修改」「设置」操作按钮，底部固定账号名与注册时间
  - 右栏分两个区块：「账号信息」用 `auto-fit` 网格平铺字段（原来是一长串分隔线列表）；「编辑昵称 / 选择头像」合并为一区
- 头像上传按钮改为固定尺寸的描边按钮（原来是无尺寸的纯文字 label，实际点击热区极小）
- 密码改用等宽字体的圆点掩码显示

#### 审批界面从弹窗改为常规页面
- `#reqPanel` 原本是 `modal-overlay` 遮罩弹窗，与侧边栏其它面板风格割裂、每次都要开关一次
- 改为 `admin` 分类下的常规面板 `#panel-approval`，带面板标题、刷新按钮与状态筛选标签
- 新增**状态筛选**：待审批 / 已批准 / 已拒绝 / 全部，各带数量角标
- 列表从单列行改为 `auto-fill` 卡片网格，状态用带底色的小标签（待审批琥珀 / 已批准绿 / 已拒绝红），审批意见单独成块展示
- 进入「管理后台」分类时默认落在「审批」（未决事项优先）

#### 新增界面圆角调节
- 设置页「主题色」下方新增「界面圆角」分组：滑块 0–24px + 直角 / 8px / 14px / 圆润 四档预设 + 实时预览 + 重置
- 以基准值 `--radius` 按比例派生 `--radius-md / sm / xs`（0 时全部归零，非 0 时小尺寸不低于 2px，保持层级差且不出现负值）
- 选择存入 `localStorage['lab_radius']`，页面加载即应用，刷新后保持
- 补齐简中 / 繁中 / 英文翻译

#### 顺带修复
- **`setLang` 中新增的重绘分支是死代码**：`setLang` / `applyI18n` 定义在主 IIFE **之外**，访问不到 IIFE 内的 `renderProfile` / `renderReqList`。已改为通过 `window.__renderProfile` / `window.__renderReqList` 桥接，并在重建面板后补一次 `applyI18n()`（渲染函数写入的文案不走 `data-i18n`）
- `data-i18n` 用于 `innerHTML` 赋值，**不能挂在带子元素的容器上**（会清空子节点）——个人主页「设置」按钮上的 `data-i18n="nav_settings"` 已移除，改由渲染时取词

## 界面统一优化 — 2026-09-24

#### 背景
前端此前处于「半迁移」状态：设计令牌已换成新版（Linear / Vercel 风格），但大量旧毛玻璃样式仍然生效——半透明白底 + `backdrop-filter: blur()` 叠在动态渐变背景上，导致卡片发灰、边界模糊、对比度不稳。为了补救可读性，又叠加了全局 `text-shadow` 白描边 hack 和 `!important` 暗色覆盖，多层样式互相打架，界面显得很乱。

本次把样式收敛成一套统一、克制的设计语言，并顺带修掉几处功能性缺陷。

#### 变更内容

**1. 设计系统重写（`public/index.html` 样式块）**
- 统一表面语言：实心卡片（`--bg2`）+ 1px 描边（`--border`）+ 分层阴影（`--shadow-*`）
- 移除全部 `backdrop-filter` 毛玻璃与 `rgba(255,255,255,.x)` 半透明表面
- 移除全局 `text-shadow` 白描边补偿（不再需要）
- 移除暗色模式的 `!important` 覆盖块，全部改由 CSS 变量驱动
- 背景光晕改为静态，去掉 20s 循环位移动画，消除持续视觉噪声
- 动效收敛：hover 只改颜色 / 描边 / 阴影，去掉位移、缩放、旋转等晃动效果
- 补齐缺失的 CSS 变量 `--bg-card`（此前被 `.avatar-dot` 引用但未定义）
- 圆角、间距、字号统一到同一套刻度

**2. 顶栏信息架构**
- 右侧 10+ 个控件改为三段分组（状态 / 消息 / 偏好），组间加细分隔线
- 品牌区去重：logo 本身已含品牌名，删掉重复的文字，改为版本号徽章
- 「模拟数据」开关默认中性样式，仅在开启时变为琥珀色胶囊（原来是常亮橙色）

**3. 仪表盘布局**
- KPI 行与图表区改为自适应网格，窄屏自动折行，不再因固定列数产生空洞
- 图表画布高度统一为 210px，卡片顶部标签不再被内联 padding 裁切

**4. 修复的功能性问题**
- **多语言切换会删掉 DOM**：`applyI18n()` 用 `innerHTML` 赋值，而 `data-i18n="panel_3d"` 写在含子元素的容器上 —— 切换语言后「远程去皮」按钮和 ESP32 状态指示会被整体清空。已将 `data-i18n` 移到内层 `<span>`。
- **站内信顶部常驻空红条**：`#mutedBanner` 内联样式里 `display` 写了两次（`none` 后又写 `flex`），后者生效导致横幅在无处罚时也显示为空框。已移除重复声明，交由 JS 控制。
- **主题色块被拉伸成 157px 巨块**：`.accent-color-grid` 用 `repeat(6,1fr)` + `aspect-ratio:1`，宽屏下色块过大。改为固定 40px 小色块。
- **界面泄漏未翻译的键名**：联系人列表在对方离线且无历史消息时显示 `msg_start_chat`（该键三种语言都未定义，`t()` 兜底返回键名本身）。已补齐简中 / 繁中 / 英文翻译。
- 硬编码颜色 `#0fbcf9` 改用 `var(--info)`，硬编码 `#666` 改用 `var(--muted)`，保证暗色模式一致。

**5. 其他**
- 聊天面板 / 群聊 / 论坛筛选栏 / 输入框等处的内联毛玻璃一并改为实心表面
- 空状态（无摄像头）改为虚线框占位，与卡片体系一致

## v2.4 — 2026-08-03

### 桌面版 + 自动更新 + KV 写入优化

#### 背景
v2.3 完成了在线状态、消息搜索和卡片化 UI。v2.4 将项目从纯 Web 应用升级为 Electron 桌面应用，支持自动更新；同时修复 KV 写入配额超限问题，优化删除操作的 KV 写入次数。

#### 变更内容

**1. Electron 桌面应用**
- 新增 `electron/main.js` 主进程：创建 1280×800 窗口，加载本地前端文件
- 新增 `electron/preload.js` 预加载脚本：通过 contextBridge 暴露 `electronAPI` 接口
- 前端自动检测运行环境（Electron vs 浏览器），动态切换 API 地址
  - 桌面端：API 请求转发到 Cloudflare Worker 远程 URL
  - 浏览器端：使用相对路径（向后兼容）
- 窗口配置：暗色背景、最小尺寸限制、macOS 隐藏标题栏风格
- 开发模式支持 DevTools 调试

**2. 自动更新机制（electron-updater + GitHub Releases）**
- 启动 3 秒后自动检查 GitHub Releases 是否有新版本
- 发现新版本 → 自动后台下载 → 下载进度通过 toast 实时显示
- 下载完成后弹出对话框，用户可选择"立即重启"或"稍后"
- 错误处理：更新失败时 toast 提示错误信息
- 前端监听更新状态：available / downloading / downloaded / error
- 发布流程：`npm run publish` → 自动构建 + 上传 GitHub Release + 生成 latest.yml

**3. 打包配置（electron-builder）**
- `package.json` 完整配置：Electron 31 + electron-builder 24 + electron-updater 6
- 构建目标：Windows NSIS 安装程序（x64）
- 安装体验：可选安装目录、创建桌面快捷方式、开始菜单快捷方式
- `electron-builder.yml` 独立配置文件
- 构建脚本：
  - `npm start` — 开发运行
  - `npm run build` — 构建安装包
  - `npm run build:portable` — 构建便携版
  - `npm run publish` — 构建并发布到 GitHub

**4. KV 写入优化（worker.js）**
- 心跳内存缓存：心跳先写入 Worker isolate 内存，每 60 秒批量刷新到 KV
  - 所有用户共用一个 `heartbeats` 键，而非每用户单独一个键
  - KV 写入次数从 N 用户 × 120 次/小时 降至 60 次/小时
- 在线状态查询改为内存读取，零 KV 消耗
- 在线判定窗口从 60 秒放宽到 90 秒（兼容跨实例延迟）
- 删除用户操作合并 KV 写入：group_messages 只读写一次（原来两次）
- 删除失败时返回友好提示而非原始 KV 错误

**5. 仪表盘图表优化**
- 环形图中心文字从"种类"改为"数量"
- "耗材剩余率"柱状图替换为"耗材类型分布"柱状图
  - 按 PLA/PETG/ABS/TPU 等类型统计数量，降序排列
  - 每种类型用不同颜色区分

#### 新增文件
- `electron/main.js` — Electron 主进程
- `electron/preload.js` — 预加载脚本
- `electron-builder.yml` — 打包配置
- `桌面版使用指南.md` — 详细使用文档

#### 使用方式
```powershell
cd d:\Trae\SparkMindsLab-v2.4
npm install        # 安装依赖
npm start          # 开发运行
npm run build      # 打包成 exe
```

---

## v2.3 — 2026-08-01

### 消息已读回执 + 在线状态 + 消息搜索 + 卡片化 UI 重构

#### 背景
v2.2 完成了文件传输修复和举报/惩罚/申诉系统全面升级。v2.3 在此基础上增强站内信即时通讯体验：新增消息已读回执、用户在线状态实时显示、全局消息搜索功能，同时对整体 UI 进行卡片化重构，提升视觉层次和交互体验。

#### 变更内容

**1. 消息已读回执（Read Receipts）**
- 后端新增 `POST /api/messages/read-receipt` 接口
  - 请求体 `{ from, to, messageIds }`，messageIds 可选
  - 标记 from→to 方向的消息为已读，记录 `readAt` 时间戳
- 前端聊天气泡新增双勾标识：
  - ✓ 单勾（灰色）：消息已发送，对方未读
  - ✓✓ 双勾（主题色）：消息已读
- 打开聊天窗口时自动发送已读回执，标记对方发来的消息为已读
- 消息轮询时同步更新已读状态，实时刷新双勾颜色
- 完整多语言支持（msg_read / msg_unread）

**2. 在线状态实时显示（Online Status）**
- 后端新增心跳机制：
  - `POST /api/heartbeat`：用户每 30 秒发送心跳，记录 `lastSeen` 时间戳
  - `GET /api/online-batch?uids=xxx,yyy`：批量查询多个用户在线状态
  - 在线判定：最后心跳在 90 秒内 → 在线（绿点）；否则 → 离线（灰点）+ 显示最后活跃时间
  - **KV 写入优化**：心跳先写入 Worker 内存缓存，每 60 秒批量刷新到 KV（所有用户共用一个 `heartbeats` 键），避免每用户每 30 秒单独写 KV 导致配额耗尽
- 前端展示：
  - 联系人列表：每个联系人头像旁显示在线状态圆点（绿色在线 / 灰色离线）
  - 联系人最后一行：在线时显示「● 在线」，离线时显示最后一条消息
  - 聊天窗口头部：对方名称旁显示在线/离线状态点
  - 鼠标悬停状态点显示 tooltip（在线 / 最后活跃: 时间）
- 心跳生命周期管理：
  - 登录成功后自动启动心跳定时器（30 秒间隔）
  - 页面关闭（beforeunload）/退出登录时自动清除心跳
- 在线状态点样式适配明暗双主题

**3. 消息搜索功能（Message Search）**
- 后端新增 `GET /api/messages/search?uid=xxx&q=keyword&peerUid=yyy` 接口
  - 搜索当前用户相关的全部私聊消息（支持可选 peerUid 过滤特定对话）
  - 关键词不区分大小写，匹配消息内容
  - 结果按时间倒序，最多返回 50 条
- 前端搜索栏：
  - 站内信面板顶部新增搜索输入框
  - 输入时 300ms 防抖触发搜索，避免频繁请求
  - 搜索结果以列表形式展示，高亮关键词（`.msg-search-highlight`）
  - 每条结果显示发送方、时间、消息内容（含文件附件标识）
  - 点击搜索结果可跳转到对应聊天窗口
  - 清空搜索框自动恢复当前聊天视图
  - 无结果时显示「未找到相关消息」提示
- 搜索结果项 hover 动画：`translateX(2px)` 位移 + 主题色背景

**4. 卡片化 UI 重构（Card-based Layout）**
- 新增 `.card-grid` CSS Grid 布局：`repeat(auto-fill, minmax(280px, 1fr))`
- 请求项（req-item）卡片化：
  - 圆角 + 阴影 + 半透明背景
  - hover 时 `translateY(-3px)` 上浮 + 阴影增强
  - 过渡动画 `transform 0.2s, box-shadow 0.2s`
- 库存卡片、元器件/主板卡片视觉层次优化
- 搜索结果项独立卡片样式（`.msg-search-result`）
- 在线状态点（`.online-dot`）：
  - 8px 圆点 + 白色/深色描边光环
  - 在线绿色发光效果（`box-shadow: 0 0 6px rgba(34,197,94,.5)`）
  - 明暗主题分别适配描边颜色
- 关键词高亮（`.msg-search-highlight`）：半透明主题色背景 + 加粗

**5. i18n 多语言更新**
- 新增 i18n 键（简中/繁中/英文三语同步）：
  - `msg_read` / `msg_unread`：已读 / 未读
  - `msg_online` / `msg_offline` / `msg_last_seen`：在线 / 离线 / 最后活跃
  - `msg_search_placeholder` / `msg_search_no_results`：搜索消息... / 未找到相关消息
- 所有新增 UI 文本均通过 `t()` 函数调用，切换语言即时更新

**6. 版本号同步**
- 前端 `index.html`：标题、品牌文本、document.title 均更新至 v2.3
- 后端 `worker.js`：版本号、API 响应 version 字段更新至 v2.3

**7. 🥚 隐藏彩蛋**
- 散落在页面各处藏有 7 个小彩蛋，等待好奇的用户发现
- 包括键盘密令、点击连击、隐藏咒语、控制台秘密、版本时间线等
- 每个彩蛋都有独特的视觉效果（彩纸雨、烟花、霓虹模式、Matrix 代码雨等）
- 不影响正常功能，仅在特定操作下触发

#### 涉及文件
| 文件 | 变更 |
|------|------|
| `public/index.html` | 已读回执 UI、在线状态显示、消息搜索、卡片化 CSS、i18n 新增、版本号、隐藏彩蛋 |
| `src/worker.js` | 已读回执 API、心跳/在线状态 API、消息搜索 API、版本号 |
| `更新日志.md` | 本文件 |
| `版本概览.md` | 版本线更新 |
| `README.md` | 更新至 v2.3 |

#### 测试要点
1. 已读回执：A 发消息给 B → A 看到单勾（灰）→ B 打开聊天 → A 刷新后看到双勾（蓝）
2. 在线状态：登录后联系人列表显示绿点；关闭页面 60 秒后变为灰点 + 最后活跃时间
3. 消息搜索：输入关键词 → 显示匹配消息列表（关键词高亮）→ 点击跳转聊天 → 清空恢复
4. 卡片化 UI：请求项/库存卡片 hover 上浮动画、搜索结果 hover 位移
5. 多语言：切换简中/繁中/英文，已读/在线/搜索相关文本正确翻译
6. 心跳清理：退出登录后心跳定时器停止，不再发送心跳请求

## v2.2 — 2026-07-26

### 文件传输修复 + 举报系统全面升级

#### 背景
v2.1 完成了毛玻璃效果和动画系统，但站内信文件传输存在 BUG（发送方和接收方均看不到文件记录），举报系统功能不够完善。v2.2 修复文件传输问题并全面升级举报/惩罚/申诉系统。

#### 变更内容

**1. 文件传输 BUG 修复**
- 问题根因：Cloudflare KV 单值上限 25MB，大文件 base64 编码后超限导致存储失败
- 解决方案：实现分块存储机制
  - 小文件（<20MB）：单键存储，与之前兼容
  - 大文件（≥20MB）：按 10MB 分块存储，下载时逐块拼接还原
  - 上传失败时自动清理已写入的分块，防止残留
- 文件大小限制提升至 50MB
- 图片自动 Canvas 压缩（最大 1920px、质量 0.85、循环降至 4MB 以内）
- 前端预检：base64 数据超 60MB 直接拒绝（50MB × 1.33 膨胀系数）
- 上传/下载全链路错误处理，失败时显示具体原因
- 文件上传状态指示器（旋转动画）

**2. 举报系统 — 管理员查看聊天记录**
- 新增 `/api/report/chat-records?uid1=xxx&uid2=yyy` 接口
- 管理员可在举报列表点击「查看聊天记录」按钮
- 弹窗显示举报者与被举报者之间的全部私聊记录（含文件附件标识）
- 消息按时间排序，左右气泡区分发送方

**3. 渐进式惩罚系统**
- 举报成立后自动按累计次数生成惩罚：
  - 第 1 次：⚠ 警告（无时长限制）
  - 第 2 次：🔇 禁言（管理员可自定义时长，默认 24 小时）
  - 第 3 次：🔇 禁言至少 720 小时（30 天）
  - 第 4 次：🔇 禁言至少 2160 小时（90 天）
  - 第 5 次及以上：🚫 永久禁用站内信和仓库操作权限
- 惩罚自动过期：mute 类型到期后自动标记为 inactive
- 禁言检查覆盖：私聊发送、群聊发送、仓库操作申请

**4. 申诉系统**
- 用户流程：在「我的处罚」面板点击「申诉」→ 输入申诉内容 → 提交
- 管理员流程：在「举报管理 → 申诉管理」标签页查看待处理申诉
  - 通过申诉：可选择撤销处罚 和/或 修改处罚原因
  - 驳回申诉：填写回复说明
- 申诉状态跟踪：pending → accepted/rejected
- 防重复提交：同一举报仅允许一条 pending 申诉

**5. 处罚展示与撤销**
- 站内信面板：被处罚用户顶部显示禁言横幅（⚠/🔇/🚫 图标 + 剩余时长 + 申诉入口链接）
- 用户管理系统：用户列表「处罚」列显示徽章（警告/禁言时长/永久禁言）
- 联系人列表：被处罚用户名旁显示处罚图标
- 「我的处罚」面板：显示活跃处罚横幅 + 全部历史处罚列表（含已过期/已撤销）+ 处罚规则说明 + 申诉入口
- 「惩罚管理」标签页：管理员查看所有活跃惩罚，可随时撤销
- 撤销处罚：标记 active=false + revokedAt 时间戳

**6. 管理员按钮清理**
- 移除管理员的「我的申请」按钮（管理员有直接操作权限，无需申请）
- 使用 DOM remove() 彻底移除而非 display:none 隐藏

**7. 其他改进**
- 举报管理三标签页：举报列表 / 申诉管理 / 惩罚管理
- 举报列表显示处罚徽章和申诉状态徽章
- 全部新增功能完整多语言支持（简中/繁中/英文）
- 版本号更新至 v2.2

#### 涉及文件
| 文件 | 变更 |
|------|------|
| `public/index.html` | 文件传输修复、举报/惩罚/申诉 UI、我的处罚面板、管理员按钮移除、版本号 |
| `src/worker.js` | 分块文件存储、举报处理 API、聊天记录 API、申诉 API、惩罚管理 API、版本号 |
| `更新日志.md` | 本文件 |
| `版本概览.md` | 版本线更新 |

#### 测试要点
1. 文件传输：A 发送文件给 B，B 能看到并下载，A 也能看到文件记录
2. 大文件传输：发送 20MB+ 文件，验证分块存储和下载还原
3. 举报流程：用户举报 → 管理员查看聊天记录 → 举报成立 → 自动生成惩罚
4. 渐进式惩罚：同一用户多次被举报成立，验证惩罚升级
5. 申诉流程：用户提交申诉 → 管理员处理 → 撤销/修改处罚
6. 处罚展示：被处罚用户在站内信看到禁言横幅，用户管理看到处罚徽章
7. 撤销处罚：管理员在惩罚管理撤销处罚，用户处罚状态实时更新
8. 管理员界面：确认「我的申请」按钮已移除

## v2.1 — 2026-07-25

### 全面毛玻璃效果 + 华丽动画系统

#### 背景
v2.0 完成了 UI 重构和设计系统搭建，但界面元素仍使用实色背景，缺乏层次感和现代感。v2.1 在此基础上全面引入毛玻璃（Glassmorphism）效果和增强动画系统，打造更华丽的视觉体验。

#### 变更内容

**毛玻璃效果（Glassmorphism）**
- 顶栏：`backdrop-filter:blur(20px) saturate(1.8)` + 半透明背景
- 侧栏：`backdrop-filter:blur(20px) saturate(1.6)` + 半透明背景
- 卡片（term-box / inv-card / room-term）：`blur(14px) saturate(1.4)` + 半透明
- 模态框：`blur(24px) saturate(1.6)` + 弹性入场动画
- 表格：`blur(10px)` + 半透明行背景
- 表单区域、详情面板、请求项、个人主页卡片均毛玻璃化
- 聊天气泡：对方消息毛玻璃化，己方消息渐变背景
- Toast 通知：毛玻璃 + 发光边框
- 所有元素明暗双主题适配

**华丽动画系统**
- 面板切换：增强位移 + 缩放过渡（`translateY(12px) scale(.98)` → 正常）
- 登录卡片：弹性入场（`cubic-bezier(.34,1.56,.64,1)`，0.6s）
- 模态框：弹性弹出（缩放 .9 → 1，位移 16px → 0）
- 聊天气泡：逐条弹性入场动画
- 库存卡片：hover 时光带扫过效果（`::after` 伪元素 + `translateX` 动画）
- 侧栏项：hover 时 `translateX(3px)` 位移，active 时左侧色条 + 渐变背景
- 按钮：hover 上浮 + 发光阴影，登录按钮光泽扫过效果
- 数量按钮：hover 时 `scale(1.1) rotate(5deg)` 旋转放大
- 头像选项：hover 时 `scale(1.15) rotate(5deg)` + 发光
- Logo：hover 时 `rotate(-12deg) scale(1.15)` + 发光滤镜
- 壁纸缩略图：hover 时 `scale(1.05)` + 发光
- 联系人项：hover 时 `translateX(2px)` 位移
- 滚动条：Indigo 主题色 + hover 变深
- 选中文本：Indigo 主题色高亮
- 背景动态光斑：`bgFloat` 20s 循环动画

**其他改进**
- 壁纸上传限制提升至 50MB，大图自动 Canvas 压缩到 2560px + 4MB 以内
- 壁纸预设名称完整多语言支持（简中/繁中/英文），切换语言后缩略图标签实时更新
- **修复 UID 复用导致新用户继承旧用户聊天记录的 BUG**：
  - UID 改用持久化计数器（`uid_counter`），删除用户后计数器不回退，新 UID 永远递增不再复用
  - 删除用户时级联清理关联数据：私聊消息、该用户发送的群消息、好友请求、其他用户好友列表中的引用、群成员资格（群主被删则解散群并清群消息）
  - 前端删除用户后刷新消息联系人列表，并显示清理统计（清除的私聊/群消息/好友请求/解散群组数量）
- **新增 UID 迁移工具（运维）**：管理员可在「数据回滚管理」面板点击「UID 迁移」按钮，将所有用户 UID 重新分配为连续编号（admin 固定 00001，其余按注册时间顺延），并同步重映射聊天/群组/群消息/好友请求中的 UID 引用，迁移前自动创建完整备份，迁移后设置 uid_counter 防止未来复用
- 版本号更新至 v2.1
- Worker.js 版本号同步更新

#### 涉及文件
| 文件 | 变更 |
|------|------|
| `public/index.html` | CSS 毛玻璃 + 动画全面升级，版本号更新 |
| `src/worker.js` | 版本号更新至 v2.1 |
| `更新日志.md` | 本文件 |
| `版本概览.md` | 版本线更新 |
| `demo.html` | 演示页面更新 |

#### 测试要点
1. 明亮模式：所有面板/卡片/模态框呈半透明毛玻璃效果，背景光斑可见
2. 暗黑模式：毛玻璃效果保持，遮罩颜色自动适配深色
3. 面板切换：有弹性缩放过渡，非生硬切换
4. 库存卡片 hover：光带扫过 + 上浮发光
5. 登录页：卡片弹性入场，按钮光泽扫过
6. 壁纸功能：上传 20MB 图片自动压缩，正常显示

## v2.0 — 2026-07-25

### UI 重构 + 设计系统 + 壁纸功能 + LOGO

#### 核心变更
- 全新 CSS 设计系统（Linear/Vercel/Stripe 风格），Indigo 主色 + 语义色 + 层级阴影
- 三段式布局：顶栏分类 + 左侧动态导航 + 内容面板
- B站风格明暗模式切换按钮（太阳/月亮动画）
- SVG 火花星标 LOGO（顶栏 24px + 登录页 60px 浮动动画）
- 自定义壁纸系统（9 款预设渐变 + 图片上传 + 透明度/模糊度/遮罩调节）
- 站内信合并群组与单聊、群组解散、举报功能、论坛系统
- 文件传输（base64，5MB 限制）
- 数据快照/回滚 API + 库存流水记录
- Cloudflare Worker + KV 存储，用户数据跨设备同步

## v1.9 — 2026-07-24

### Cloudflare Workers 迁移

- 从 Node.js 服务器迁移至 Cloudflare Workers
- 用户数据存储至 Cloudflare KV（namespace: USERS）
- 好友系统、站内信、管理员广播
- 管理员审批授权机制

## v1.4 — 2026-07-09

### 用户数据跨设备持久化

- localStorage → 服务端 data/users.json + REST API
- 新增 /api/login, /api/users (GET/POST/DELETE) 端点

## v1.3 — 2026-07-09

### Bug 修复

- 用户列表空数组回退默认用户
- 新增用户密码框改掩码

## v1.2 — 2026-07-09

### 库存数量融合

- 元器件/主板卡片数量改为 qty 总和，与详情表格实时联动

## v1.1 — 2026-07-09

### 权限策略调整

- 普通用户禁止控制灯光，仅可查看数据
- 服务端增加命令权限审计

## v1.0 — 2026-07-08

### 初始版本

- 传感器监测、灯光控制、材料/仓库/用户管理
- admin/user 双角色权限
