# SparkMinds Lab 桌面版使用指南

## 环境准备

### 1. 安装 Node.js
下载地址：https://nodejs.org/ （选择 LTS 版本）

验证安装：
```powershell
node -v
npm -v
```

### 2. 安装依赖
在项目根目录执行：
```powershell
cd d:\Trae\SparkMindsLab-v2.4
npm install
```

## 开发模式运行

```powershell
npm start
```
这会启动 Electron 窗口加载本地 `public/index.html`，API 请求转发到 Cloudflare Worker。

## 打包成安装程序

### 构建 Windows 安装包 (.exe)
```powershell
npm run build
```
生成的安装包在 `release/` 目录下，文件名格式：`SparkMindsLab-Setup-2.4.0.exe`

### 构建便携版 (免安装)
```powershell
npm run build:portable
```

## 自动更新配置

### 前提条件
1. GitHub 账号
2. 创建 GitHub 仓库 `sparkminds-lab`（或修改 `package.json` 中的 publish 配置）

### 发布新版本流程

1. 修改 `package.json` 中的 `version` 字段（如 `"version": "2.4.1"`）

2. 设置 GitHub Token（用于发布 Release）
   ```powershell
   # 在 GitHub Settings > Developer settings > Personal access tokens 生成
   # 勾选 repo 权限
   $env:GH_TOKEN="你的GitHub Token"
   ```

3. 发布
   ```powershell
   npm run publish
   ```
   这会自动：
   - 构建安装包
   - 上传到 GitHub Releases
   - 生成 `latest.yml`（自动更新所需的版本信息文件）

4. 用户端自动更新
   - 已安装的旧版本启动时自动检查 GitHub Releases
   - 发现新版本后自动下载
   - 下载完成弹出提示，用户选择立即重启或稍后

## 修改 Cloudflare Worker URL

如果 Worker 部署地址变更，修改 `public/index.html` 第 1664 行：
```javascript
const WORKER_URL = 'https://你的Worker地址.workers.dev';
```

## 修改 GitHub 仓库配置

如果使用不同的 GitHub 仓库，同时修改以下文件：
- `package.json` → `build.publish`
- `electron-builder.yml` → `publish`

## 常见问题

### Q: 打开后白屏？
A: 检查 `public/index.html` 是否存在，以及 `electron/main.js` 路径是否正确。

### Q: API 请求失败？
A: 确认 Cloudflare Worker 已部署，且 `WORKER_URL` 地址正确。

### Q: 自动更新不生效？
A: 1) 确认 GitHub Release 已发布  2) 确认 `latest.yml` 已上传  3) 确认版本号比当前安装版本大

### Q: Windows SmartScreen 警告？
A: 未签名的应用会显示此警告，点击"仍要运行"即可。如需消除，需购买代码签名证书（约 $200/年）。
