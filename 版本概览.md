# SparkMinds Lab 版本概览

## 项目简介
SparkMinds Lab 是一个实验室物联网监控管理平台，包含传感器实时监测、灯光控制、3D 打印材料管理、仓库元器件/主板管理、用户管理、站内信（含群聊/文件传输/已读回执/在线状态/消息搜索）、举报/惩罚/申诉系统、论坛等功能。v2.4 起支持 Electron 桌面应用 + 自动更新。前端为单页 HTML（含内嵌逻辑），后端为 Cloudflare Worker + KV 存储。

## 版本线

| 版本 | 日期 | 核心变更 | 文件夹 |
|------|------|----------|--------|
| v1.0 | 2026-07-08 | 初始版本：传感器监测、灯光控制、材料/仓库/用户管理、admin/user 双角色 | （上传原始文件） |
| v1.1 | 2026-07-09 | 权限策略调整：普通用户禁止控制灯光，仅可查看数据；服务端增加命令权限审计 | `SparkMindsLab-v1.1` |
| v1.2 | 2026-07-09 | 库存数量融合：元器件/主板卡片数量改为 qty 总和，与详情表格实时联动 | `SparkMindsLab-v1.2` |
| v1.3 | 2026-07-09 | Bug修复：用户列表空数组回退默认用户；新增用户密码框改掩码 | `SparkMindsLab-v1.3` |
| v1.4 | 2026-07-09 | 用户数据跨设备持久化：localStorage → 服务端 data/users.json + REST API | `SparkMindsLab-v1.4` |
| v1.9 | 2026-07-24 | Cloudflare Workers 迁移：KV 存储、好友系统、站内信、管理员广播、审批授权 | `SparkMindsLab-v1.9` |
| v2.0 | 2026-07-25 | UI 重构 + 设计系统 + 壁纸功能 + LOGO + 群组/论坛/举报/文件传输 | `SparkMindsLab-v2.0` |
| v2.1 | 2026-07-25 | 全面毛玻璃效果 + 华丽动画 + UID 复用修复 + UID 迁移工具 | `SparkMindsLab-v2.1` |
| v2.2 | 2026-07-26 | 文件传输分块存储修复 + 举报系统全面升级（聊天记录/渐进式惩罚/申诉/处罚展示） | `SparkMindsLab-v2.2` |
| v2.3 | 2026-08-01 | 消息已读回执 + 在线状态实时显示 + 消息搜索 + 卡片化 UI 重构 | `SparkMindsLab-v2.3` |
| v2.4 | 2026-08-03 | Electron 桌面应用 + 自动更新 + KV 写入优化 + 仪表盘图表优化 | `SparkMindsLab-v2.4` |

## 当前版本 v2.4 文件清单

| 文件 | 说明 |
|------|------|
| `public/index.html` | 前端主程序（UI + 逻辑 + Electron 环境检测 + 自动更新监听 + 全部功能） |
| `src/worker.js` | Cloudflare Worker（API 路由 + KV 存储 + 心跳内存缓存 + 删除操作优化） |
| `electron/main.js` | Electron 主进程（窗口创建 + 自动更新逻辑） |
| `electron/preload.js` | Electron 预加载脚本（contextBridge API 暴露） |
| `electron-builder.yml` | electron-builder 打包配置（NSIS + GitHub 发布） |
| `wrangler.toml` | Cloudflare Workers 配置（KV namespace: USERS） |
| `package.json` | 项目依赖 + Electron 构建脚本 |
| `桌面版使用指南.md` | 桌面版详细使用文档 |
| `更新日志.md` | 本版本详细变更记录 |
| `版本概览.md` | 版本总览（本文件） |
| `README.md` | 项目说明 |

## 部署方式

### Web 版（Cloudflare Workers）
```bash
cd SparkMindsLab-v2.4
npx wrangler deploy    # 部署至 Cloudflare Workers
```

### 桌面版（Electron）
```powershell
cd SparkMindsLab-v2.4
npm install            # 安装依赖
npm start              # 开发运行
npm run build          # 打包成 Windows 安装程序
npm run publish        # 打包并发布到 GitHub Releases（触发自动更新）
```

- Web 版：前端和 API 均部署在 Cloudflare Workers，数据存储在 KV
- 桌面版：前端打包在本地，API 请求转发到 Cloudflare Worker 远程 URL
- 自动更新：通过 GitHub Releases 分发，electron-updater 检查并下载新版本

## 默认账号
| 用户名 | 密码 | 角色 | 权限 |
|--------|------|------|------|
| admin | admin123 | admin | 全部权限（管理用户/库存/举报/惩罚/审批/广播） |
| user | user123 | user | 仅查看数据 + 站内信/群聊/论坛（受处罚状态限制） |

## 权限模型（v2.2+）
- **admin**：完整权限，可控制灯光、管理材料/仓库/用户、处理举报/申诉/惩罚、发送广播、直接操作仓库（无需申请）
- **user**：只读数据 + 站内信/群聊/论坛/文件传输，仓库操作需提交申请等待审批
- **被处罚用户**：
  - ⚠ 警告：可正常使用所有功能，但有警告记录
  - 🔇 禁言：禁止发送私聊/群聊消息、禁止提交仓库操作申请
  - 🚫 永久禁言：永久禁止发送消息和提交申请
  - 管理员可随时撤销任何处罚

## 站内信系统（v2.3+）

### 即时通讯功能
- **私聊/群聊**：5 秒轮询实时收发消息，支持文件附件
- **已读回执**：消息气泡显示 ✓（未读）/ ✓✓（已读）双勾标识
- **在线状态**：联系人列表和聊天窗口显示绿点（在线）/灰点（离线）+ 最后活跃时间
- **消息搜索**：站内信面板顶部搜索栏，关键词高亮，300ms 防抖，点击跳转聊天
- **文件传输**：最大 50MB，图片自动压缩，KV 分块存储（≥20MB 按 10MB 分块）

### 举报/惩罚/申诉系统（v2.2+）

#### 举报流程
1. 用户在聊天或论坛点击「举报」→ 选择原因 + 填写详情 → 提交
2. 管理员在「举报管理 → 举报列表」查看举报
3. 管理员可点击「查看聊天记录」查看举报者与被举报者的全部私聊
4. 管理员处理：举报成立（resolved）或 驳回（dismissed）

#### 渐进式惩罚（仅举报成立时触发）
| 累计次数 | 惩罚 | 时长 |
|----------|------|------|
| 第 1 次 | ⚠ 警告 | 无限制 |
| 第 2 次 | 🔇 禁言 | 管理员自定义（默认 24h） |
| 第 3 次 | 🔇 禁言 | 至少 720h（30 天） |
| 第 4 次 | 🔇 禁言 | 至少 2160h（90 天） |
| 第 5 次+ | 🚫 永久禁言 | 永久（站内信 + 仓库操作） |

#### 申诉流程
1. 被处罚用户在「我的处罚」面板点击「申诉」→ 输入申诉内容 → 提交
2. 管理员在「举报管理 → 申诉管理」查看待处理申诉
3. 管理员处理：
   - 通过申诉：可撤销处罚 和/或 修改处罚原因
   - 驳回申诉：填写回复说明

### 处罚展示
- 站内信面板：被处罚用户顶部显示禁言横幅
- 用户管理系统：用户列表「处罚」列显示徽章
- 联系人列表：被处罚用户名旁显示图标
- 「我的处罚」面板：活跃处罚 + 历史记录 + 申诉入口
- 「惩罚管理」标签页：管理员查看/撤销所有活跃惩罚

## 文件传输系统（v2.2+）
- 最大文件大小：50MB
- 图片自动压缩：Canvas 压缩至 1920px + 4MB 以内
- 存储方式：Cloudflare KV 分块存储（≥20MB 按 10MB 分块）
- 支持私聊文件、群聊文件

## API 路由总览（v2.3+）

### 用户认证与管理
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/login | 登录 |
| GET | /api/users | 获取用户列表（?with_pass=1 含密码） |
| POST | /api/users | 新增用户 |
| DELETE | /api/users?name=xxx | 删除用户（级联清理） |
| PUT | /api/users | 修改密码 |
| GET | /api/profile?uid=xxx | 按 UID 查询用户 |
| PUT | /api/profile | 更新昵称/头像 |

### 好友系统
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/friends?uid=xxx | 获取好友列表 |
| POST | /api/friends/request | 发送好友请求 |
| POST | /api/friends/accept | 接受好友请求 |
| POST | /api/friends/reject | 拒绝好友请求 |
| GET | /api/friends/requests?uid=xxx | 获取收到的好友请求 |
| POST | /api/friends/remove | 删除好友 |

### 站内信（v2.3 新增标注 ✨）
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/messages?uid=xxx&peerUid=yyy | 获取聊天记录 |
| POST | /api/messages | 发送私聊消息 |
| POST | /api/messages/broadcast | 管理员广播 |
| GET | /api/messages/unread?uid=xxx | 未读消息数 |
| GET | /api/messages/contacts?uid=xxx | 联系人列表 |
| ✨ POST | /api/messages/read-receipt | 发送已读回执 |
| ✨ GET | /api/messages/search?uid=&q=&peerUid= | 搜索私聊消息 |

### 在线状态（v2.3 新增 ✨）
| 方法 | 路径 | 说明 |
|------|------|------|
| ✨ POST | /api/heartbeat | 用户心跳（30s 间隔） |
| ✨ GET | /api/online-batch?uids=xxx,yyy | 批量查询在线状态 |

### 举报/惩罚/申诉系统
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/report | 提交举报 |
| GET | /api/reports | 获取举报列表 |
| POST | /api/report/handle | 处理举报 |
| GET | /api/report/chat-records?uid1=&uid2= | 查看聊天记录 |
| POST | /api/punishment/create | 创建惩罚 |
| GET | /api/punishments | 获取惩罚列表 |
| POST | /api/punishment/revoke | 撤销惩罚 |
| GET | /api/my-punishments?uid=xxx | 获取我的处罚 |
| POST | /api/appeal/submit | 提交申诉 |
| GET | /api/appeals | 获取申诉列表 |
| POST | /api/appeal/handle | 处理申诉 |

### 申请授权系统
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/requests | 获取申请列表 |
| POST | /api/requests | 提交申请 |
| POST | /api/requests/approve | 管理员批准 |
| POST | /api/requests/deny | 管理员拒绝 |
| GET | /api/requests/pending | 待处理数量 |

### 数据管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/snapshots | 获取备份列表 |
| POST | /api/snapshot | 创建备份 |
| POST | /api/restore | 恢复备份 |
| DELETE | /api/snapshot?id=xxx | 删除备份 |
| POST | /api/uid-migrate | UID 迁移 |
| GET | /api/transactions | 获取库存流水 |
| GET | /api/stats | 存储统计 |

## 技术栈
- 前端：原生 HTML/CSS/JavaScript（单页应用，内嵌全部逻辑）
- 后端：Cloudflare Workers（Serverless）
- 存储：Cloudflare KV（namespace: USERS）
- 通信：HTTP REST API
- Web 部署：Cloudflare Workers + Workers Assets
- 桌面端：Electron 31 + electron-builder 24
- 自动更新：electron-updater 6 + GitHub Releases
- 桌面端 API 通信：远程 Worker URL（CORS 已配置）
